# Ver.4.3 表示の整理

- 「この記事は何段ですか？」を削除しました。
- 段数をAPIへ送る処理を削除し、記事整理AIへ「画像から段組みを判断してください」と指示します。
- 緑色の「記事理解AIの確認」（概要・長い確認箇所一覧）を画面から削除しました。
- 元記事と編集する文章を並べて表示し、選んだ行だけ拡大画像で照合する機能を維持します。
- 読み取り後の内部の順序整理は維持します。今回、修正候補ボタンが無効になる問題と切り抜きの拡大表示は変更していません。

既存の18項目の内部テストを実行済みです。新しい版の全体ビルド・実画面はCloud Shellと公開前の確認で検証してください。
環境変数、Cloud Run設定、依存パッケージの変更はありません。

ZIPを別のフォルダーに展開し、その中で実行します。

```bash
npm ci
npm run build
```

確認後、ZIPの中身をGitHubのmainブランチの20260907chatGTPVer内へ更新します。
backup-ver4.1は保存版として残してください。
